package com.org.scarlet.coroutines.advanced

import kotlinx.coroutines.runBlocking
import org.junit.Test

class SuspendingCoroutineTest {

    @Test
    fun `callback test`() {
        // Arrange (Given)

        // Act (When)

                // Assert (Then)

    }

    @Test
    fun `wrapper suspending function V1 test`() = runBlocking {
        // Arrange (Given)

        // Act (When)

        // Assert (Then)
    }

    @Test
    fun `wrapper suspending function V2 test`() = runBlocking{
        // Arrange (Given)

        // Act (When)

        // Assert (Then)
    }
}
